# V4 change log

## Learning system giữ từ v3

- 20/45/90-minute modes;
- WIP=1;
- recovery without reset/debt;
- Predict/Execute/Explain/Diagnose/Transfer gate;
- H1-H5 staged hints;
- no-agent transfer and seeded-failure evidence;
- canonical Laundry guardrails.

## Teaching layer được xây lại

- zero no-skip entry instead of async placement test;
- 13 foundation chapters plus capstone;
- strict TypeScript teaching and typecheck labs;
- QA/test design before tool-first automation;
- expanded Playwright core with dedicated learner specs;
- API/SQL/Git/CI/Docker operational modules;
- Java reading bridge and full Spring reverse trace;
- eight SWP waves covering UC-01 through UC-30.

## Personalization

- internship runway almost three months;
- SWP starts in roughly three weeks and runs two months;
- full-system learning rather than member-only ownership;
- true-zero assumption after long AI dependence;
- technical English maintenance from an IELTS ~6.5 self-assessment;
- job discovery/application excluded from core study time;
- CI before Docker, Docker only operational.

## Delivery changes

- three separate PDFs: teaching, 12-week playbook, hints/rubrics;
- expanded runnable labs and explicit status command;
- structural validator checks teaching depth, canonical coverage and PDF navigation;
- v3 preserved as a separate package.

## Teaching-lab synchronization repair - 13/08/2026

- replaced topic-only foundation contracts with the exact QA00/JS01-JS08/TS01-TS04
  Completion and Independent tasks taught in PDF 01;
- added real browser bridges for JS08/TS04 and made the sandbox order detail expose the
  customer/status oracles those lessons require;
- made P05 deliberately reproduce shared-owner pollution, added four P09 bug notes and a
  runnable P11 starter plus planning/evidence cards;
- added concrete J01 design/mutation, J02 trace, J03 PostgreSQL SQL, J04 disposable Git,
  J06 Compose and J07 communication artifacts;
- added `source/lab_contracts_v4.json`; validator now fails on missing teaching paths,
  artifacts or npm scripts instead of accepting topical filename matches;
- removed stale F00-F08 filesystem aliases from the learner-facing map.

## JS01 zero-prerequisite repair - 20/08/2026

- explained that comparison expressions produce booleans without `if/else`;
- rewrote the Independent task around the exact existing artifact, explicit edit boundary,
  prediction-run-mutate-observe-revert sequence and final gate state;
- removed premature `if`, `throw`, `import.meta`, `process.argv`, function and array syntax
  from JS01 worked/learner artifacts while preserving learner-entered expressions/data;
- changed invalid-number work to observable validity flags; branching moves to JS02 and
  thrown-error handling remains in JS05;
- isolated deliberate failures in `js01_failure_playground.mjs`, replaced the premature
  object/property typo with a variable-binding `ReferenceError`, and rewrote JS01 H1-H5 so
  the companion no longer teaches transition/function/error logic before JS02-JS05;
- changed PDF running headers to stable volume labels so chapter-opening pages cannot inherit
  the previous chapter's name;
- rebuilt all three PDFs and visually checked the affected Teaching and Hints pages.

## Full true-zero curriculum refactor - 28/08/2026

- audited every concept lesson instead of patching JS01 alone: 39 QA/JS/TS/Playwright/job/Spring
  lessons now expose an explicit mental model, worked example, prediction, completion,
  independent task, failure injection, transfer and gate;
- removed forward syntax leaks from the foundation runway: JS02 is a plain sequential
  condition/loop script, JS03-JS05 stop borrowing later object/async syntax, TS01 no longer
  borrows `readonly`, and TS03 validates `unknown` without pretending a cast is validation;
- moved the event-loop prediction lab from JS02 to JS06, added learner-owned TS01/TS02/TS04
  negative type-test scaffolds, and made JS08/TS04 require both Node contracts and real browser
  specs;
- added a guarded focused runner for JS08, TS04 and P01-P11: a target containing
  `test.fixme`/`test.skip` now exits non-zero instead of reporting a misleading green run;
- expanded P11 and all J00/S01-S07 lessons to the same zero-ready shape, split shared Spring
  evidence cards into one file per lesson, and added OPEN W1-W8 evidence cards that cannot be
  earned by an Agent patch;
- expanded the teaching-to-lab manifest from 29 grouped/topic contracts to 47 individual
  contracts and added `npm run zero:audit` to catch missing blocks, thin hint ladders, stale
  paths, prerequisite leaks, grouped Spring evidence and false-green runners;
- rewrote the three-week career routing around the real 55-62 active-hour foundation estimate:
  SWP can begin on the calendar while foundation continues, but direct prerequisites may not be
  skipped or marked complete by schedule pressure;
- made J03 operational for a zero-background learner with exact Compose/health/psql commands,
  concrete result oracles and safe reset boundaries; aligned all worked SQL identifiers with the
  disposable schema and verified the reference queries on a fresh temporary PostgreSQL cluster;
- preserved the learning ledger exactly: QA00 remains passed, JS01 remains current, and no
  maintenance-generated artifact counts as learner mastery.
