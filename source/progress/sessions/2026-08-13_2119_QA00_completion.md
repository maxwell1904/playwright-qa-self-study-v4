# Session evidence note

- Date / duration: 2026-08-13 and 2026-08-18 Asia/Ho_Chi_Minh; duration not recorded
- Mission: QA00 runtime/terminal/npm feedback
- Mode: not recorded
- AI level: not recorded
- One observable outcome: Learner replaced the three receipt TODO values, ran the learner lab successfully, independently invoked the QA00 reference suite, and verified exit status 0.

## Closed-book start

- What I remember: Output follows source order; npm uses the project package script; exit code 0 means clean completion.
- My prediction: Renaming the target file would cause a module/file-loading failure; running from `foundations/work` would make npm search upward for the package root; correct output with exit code 1 would still be failure. For the reference command, learner corrected the status prediction and verified exit code 0.
- Why: The command must pass through shell/npm, Node module loading, then the test assertions.
- Confidence 0-100%: not recorded

## Work evidence

- Command/action: Replaced `TODO` values in `lab-kit/foundations/work/qa00.mjs`; ran `npm run lab:qa00`; then ran `npm run lab:qa00:reference && echo $?` from `lab-kit`.
- Expected: Exact receipt output and clean test run; reference runner should finish successfully and report no new artifact.
- Actual: Learner suite reported `tests 2`, `pass 2`, `fail 0`. Reference suite reported `tests 2`, `pass 2`, `fail 0`, followed by `0`. First meaningful output: `Foundation target: reference`. No new artifact or folder observed.
- Artifact/trace/log path: `lab-kit/foundations/work/qa00.mjs`; no new reference artifact.

## Debug journal

- First meaningful error: `npm error code ENOENT`; npm could not read `/Users/maxwell/FPTu_Muon_Nam/fso_learning/Playwright_QA_SWP391_Self_Study_v4/package.json`
- Hypothesis 1: npm may report a missing script.
- Hypothesis 2: the current directory is outside the package root, so npm cannot find `package.json`.
- Cheapest decisive evidence: the error path named the parent directory's missing `package.json`; `pwd` showed the shell had left `lab-kit`.
- Evidence observed: npm failed at the package-loading layer before it could look up `lab:qa00:reference`; using `; echo $?` captured exit status `254`. After `cd lab-kit`, the same reference command reported 2/2 pass and exit status `0`, proving the correction. A second injection with misspelled script `lab:qa00:referenc` produced `Missing script` and exit status `1`; npm suggested the correct script but did not run tests. A third injection removing the closing backtick produced `SyntaxError: Unexpected token ':'` and exit status `1`; both tests failed, with the syntax error as root cause. A fourth injection renaming `qa00.mjs` produced `ERR_MODULE_NOT_FOUND` for the expected work path and exit status `1`; the second failure was a downstream child-process assertion.
- Root cause: first injection used a directory without `package.json`; second used a script name absent from `package.json`; third malformed a template literal in the learner file; fourth renamed the contract-required learner file.
- Smallest fix: `cd lab-kit`.
- Regression proof: #1 and #2 corrections have green proof; #3 and #4 correction proofs pending.

## Transfer

- Same idea on a different task/flow: not attempted
- What changed: not attempted
- What stayed invariant: not attempted

## Teach-back

- Explain data/object/state/timeline in 3-5 sentences: Learner explained Node as the JavaScript/TypeScript runtime, npm as project/package management, npx as a local tool launcher, and distinguished syntax, module-loading, and missing-script failures. Learner explained `work` as learner code and `reference` as completed code. Learner transferred the project-root idea to Maven and named `pom.xml`.
- One tempting wrong explanation: npm manages arbitrary project folders/files; module error simply means a folder is wrong.
- One question I still cannot answer: none after repair; Maven uses `pom.xml` for project/build/dependency declaration, while npm uses `package.json` plus lockfile for analogous project metadata and resolved dependencies.

## Gate

- Predict 0/1/2: 2 based on the written predictions and status corrections
- Execute 0/1/2: 2; learner completed work, independent task, four failure injections, corrections, and final learner proof
- Explain 0/1/2: 2; learner corrected npm scope and package-lock meaning after feedback
- Diagnose 0/1/2: 2; learner separated package-loading, script lookup, syntax, and module-not-found failures
- Transfer 0/1/2: 2; learner stated the root/command mental model transfers to Maven and identified `pom.xml`
- Total /10: 10/10
- Decision: pass

## Restart

> Mở `lab-kit/foundations/work/js01.mjs`, ghi prediction cho ba biểu thức, rồi chạy `npm run lab:js01`.
