import { test, expect } from '@playwright/test';

test.fixme('JS08 completion: visitor sees the ironing service by semantic business identity', async ({ page }) => {
  // TODO 1: navigate to /services.
  // TODO 2: locate the row containing the service name "Ủi quần áo", never by index.
  // TODO 3: assert ITEM and 12.000 ₫ inside that same row.
  // TODO 4: cause one assertion failure, preserve its call log, then restore the oracle.
  void page;
  void expect;
});

test.fixme('JS08 independent: opens LD-001 correctly after descending reorder', async ({ page }) => {
  // Open /orders?order=desc, scope the row by LD-001, click its Chi tiết link,
  // then prove the detail belongs to LD-001 and not LD-002.
  void page;
  void expect;
});
