import test from 'node:test';
import assert from 'node:assert/strict';
import { normalizeStoredPhone, deriveBalance } from './js07.mjs';

test('TODO JS07: replace this with three explicit phone behavior tests', () => {
  void normalizeStoredPhone;
  assert.fail('TODO: write the phone tests before asking for H3');
});

test('TODO JS07 independent: derive the ledger cases from a decision table', () => {
  void deriveBalance;
  assert.fail('TODO: cover no rows, collections, refund, clamp, unknown type and no mutation');
});
