const orderCode = 'LD-20260813-000123';
const customerName = 'Trần Thu Hà';
const collectedAmount = 50000;
const orderTotal = 120000;

// Replace only the three TODO expressions.
export const remainingAmount = 0; // TODO derive from orderTotal and collectedAmount
export const fullyPaid = true; // TODO derive from remainingAmount
export const label = ''; // TODO derive from the existing bindings

console.log(remainingAmount);
console.log(fullyPaid);
console.log(label);

export { orderCode, customerName, collectedAmount, orderTotal };
