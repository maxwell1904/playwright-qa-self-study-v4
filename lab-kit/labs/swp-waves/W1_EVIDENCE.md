# W1 evidence - scaffold and first vertical slice

Status: OPEN

Implementation repo / branch / commit:

UC / BR in scope:

Prediction and highest risks:

Empty PostgreSQL -> Flyway -> Hibernate validation command/evidence:

`/services` Controller -> service -> repository -> table -> view trace:

Inactive-service and VND/unit negative proof:

Focused service / repository / web / Playwright commands:

Deliberate failure and first rejecting layer:

Clean-run result:

No-agent teach-back verdict:

Gate: OPEN
