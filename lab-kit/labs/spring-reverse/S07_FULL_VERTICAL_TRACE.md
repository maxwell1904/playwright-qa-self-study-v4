# S07 full vertical trace and random-change card

Repo / commit:

Actor + observable outcome + UC / BR:

Prediction before run:

Browser DOM / action / oracle:

HTTP method / path / query / body / cookie / CSRF:

Container and security decision:

DispatcherServlet / mapping / binding / validation:

Controller method and navigation:

Service command / query / transaction / lock / rules:

Repository / JPA / SQL:

PostgreSQL tables / constraints / affected rows:

Response status / Location / model / view:

Final HTML / DOM oracle:

Disturbed variable and predicted first rejecting layer:

Actual evidence and no-partial-write proof:

Random change blast radius / tests / rollback point:

Three-minute no-note teach-back:
