# S04 Flyway, JPA and PostgreSQL trace

Repo / commit and query / BR:

Output row grain and stable order prediction:

Flyway migration / table / columns / constraints / indexes:

Entity mapping and enum / money / time types:

Repository method / SQL / parameters:

Expected cardinality and pagination / count contract:

JOIN multiplication risk and mitigation:

Business-zone to half-open instant range:

PostgreSQL-specific behavior being proved:

Empty-database migration + Hibernate validation evidence:

Boundary seed and expected rows / aggregate:

Actual SQL / result evidence:

Focused repository test:
