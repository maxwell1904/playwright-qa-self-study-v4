# S01 HTTP and Servlet-container trace

Use only with one concrete request in a real Laundry implementation repo.

Repo / commit:

Prediction before run:

Actor and observable outcome:

HTTP method / path / query / body:

Listener / container evidence:

Request and response objects observed:

Filter-chain outcome:

DispatcherServlet and selected handler:

Service / repository / SQL reached:

View / status / redirect:

First differing layer for one 404, 302, 403 or 500 variation:

Browser / MockMvc evidence:

Three-minute teach-back result:
