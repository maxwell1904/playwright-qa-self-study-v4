# S02 MVC binding, validation and PRG trace

Use one card for one GET-form + POST-form pair in a real Laundry implementation repo.

Repo / commit and UC / BR:

Prediction for malformed, shape-invalid, business-conflict and valid input:

GET form route / model / view:

POST route and submitted browser-owned fields:

Server-derived actor / code / money / status / time:

Argument resolution and typed form:

Binding / conversion / Bean Validation result:

BindingResult placement and retained safe input:

Service command and business-conflict mapping:

Success POST status / Location / flash:

Redirect GET status / view / DOM oracle:

Failure case proving the service was not called:

MockMvc / browser evidence:
